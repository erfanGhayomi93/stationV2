import React from 'react';
import { Envelope } from 'src/common/icons';

const Messages = () => {
    //
    return (
        <div className="px-2 text-green-500 hover:text-green-600 hover:cursor-pointer">
            <Envelope />
        </div>
    );
};

export default Messages;
