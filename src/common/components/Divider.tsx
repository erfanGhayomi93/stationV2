const Divider = () => {
     return (
          <div
               style={{
                    minHeight: '1px',
               }}
               className="w-full bg-line-div-2"
          />
     );
};

export default Divider;
